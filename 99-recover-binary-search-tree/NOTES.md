### Brute force approach with sc : O(N) && tc : O(NlogN)
​
find inorder traversal of the given bst and sort it (takes **O(NlogN)** )
then again traverse inorder replace nodes which not correctly placed